import { FallbackImageDirective } from './fallback-image.directive';

describe('FallbackImageDirective', () => {
  it('should create an instance', () => {
    const directive = new FallbackImageDirective(null);
    expect(directive).toBeTruthy();
  });
});
